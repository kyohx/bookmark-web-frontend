# 環境変数設定ファイル (.env.local)
#
# このファイルをコピーして .env.local を作成してください:
#   cp .env.example .env.local
#
# .env.local はGitで管理されないため、環境ごとに異なる設定が可能です。

# ========================================
# バックエンドAPI URL設定
# ========================================

# 開発環境（Viteプロキシを使用する場合）
# コメントアウトまたは空にすると、vite.config.tsで設定されたプロキシが使用されます
# VITE_BACKEND_API_URL=

# ローカル開発（プロキシを使用しない場合）
# VITE_BACKEND_API_URL=http://localhost:8000

# ステージング環境
# VITE_BACKEND_API_URL=https://api-staging.example.com

# 本番環境
# VITE_BACKEND_API_URL=https://api.example.com

# ========================================
# 使用方法
# ========================================
# 1. 上記の設定例から使用する環境の行のコメントを外す
# 2. URLを実際の環境に合わせて変更
# 3. 開発サーバーを再起動（環境変数の変更はサーバー再起動が必要）
